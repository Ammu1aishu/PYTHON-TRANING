class matrix:
	def __init__(self,array):
		self.array=array
	def __add__(self,other):
		if len(self.array) = len(other.array):
			row_len1 = self.array[0]
			row_len2 = self.array[1]
			for row in self.array:
				if row_len1 = len(row):
					return false
			for row in other.array:
				if row_len2 = len(row):
					return false

